# CLAUDE.md

このファイルはClaudeが判断を下す際の最重要ルールを定義します。
詳細なコーディング規約は `.claude/rules/` を参照してください。

---

## セッション開始プロトコル

**作業開始前に必ず `.claude/revision_log.md` を読み、過去のミスパターンを把握してから作業を始める。**

---

## 最重要6原則

### 1. Plan Mode Default（計画先行）

3ステップ以上のタスクは実装前に計画を提示し、承認を得てから進む。

- チェック：「このタスクは3ステップ以上か？」→ Yes なら計画を提示して承認を得る

### 2. Self-Improvement Loop（自己改善ループ）

ミスのパターンを `.claude/revision_log.md` に記録し、毎セッション冒頭で読み返す。

- チェック：「今回のミスを revision_log.md に記録したか？」→ No なら記録する

### 3. Verification Before Done（完了前検証）

完了前に「スタッフエンジニアが承認するレベルか？」を自問する。

- チェック：「シニアエンジニアのレビューで指摘される点はないか？」→ あれば修正してから完了報告する

### 4. Subagent Strategy（サブエージェント委譲）

リサーチ・分析はサブエージェントに委譲し、メインコンテキストを保全する。

- チェック：「この作業はリサーチ・広範囲の調査・分析か？」→ Yes なら `Agent` ツールに委譲する

### 5. Demand Elegance（エレガンスの要求）

設計判断を含む変更では、力技の前に2〜3のアプローチを比較検討する（細かい修正は除く）。

- チェック：「この変更に設計判断が伴うか？」→ Yes なら複数アプローチを比較提示する

### 6. Autonomous Bug Fixing（自律的バグ修正）

バグ報告時はまず自律的に調査・修正し、設計判断のみ確認を取る。

- チェック：「これは明確なバグか、それとも設計判断を要するか？」→ 明確なバグなら自律修正する

---

## 詳細ルール参照先

| カテゴリ | ファイル |
|---|---|
| コーディング規約 | `.claude/rules/coding.md` |
| テスト方針 | `.claude/rules/testing.md` |
| Git workflow | `.claude/rules/git.md` |
| 自動処理設定 | `.claude/settings.json` |
| ミス記録 | `.claude/revision_log.md` |
